# Liver Atlas Capstone — Tristan Jones, Spring 2026

## Project Structure


```
Project/
  Registration/              # Utility library — imported by Atlas pipeline
    Config.py                # All tunable parameters
    Run.py                   # Legacy CLI for pairwise TPS registration (kept for comparison)
                             #   python -m Registration.Run --ref 0004 --src 0010
    stages/
      load.py                # Load NIfTI segmentations from segmentations.zip or disk
      landmarks.py           # Extract anatomical landmark clusters (Couinaud + vascular)
      align.py               # Rigid alignment: affine-direction rotation + centroid
                             #   translation + volume scale (replaces Procrustes)
      tps.py                 # Thin Plate Spline — legacy, not called by atlas pipeline
      evaluate.py            # Apply TPS transform + Dice — legacy
    utils/
      Checkpoint.py          # Save/load intermediate stage results
      Nifti.py               # Shared helpers: voxels_to_mm, mm_to_voxels

  Atlas/                     # Probabilistic liver atlas
    utils.py                 # Shared geometry helpers + Plotly building blocks
    registration.py          # Rigid alignment wrapper + cache
                             #   Applies: affine rotation → centroid translation → volume scale
                             #   Caches per-patient alignment to outputs/reg_cache/*.npz
    liver_atlas.py           # Two-pass atlas build:
                             #   Pass 1 — compute extents + voxel counts + direction matrices
                             #            → saved to outputs/reg_cache/extents_{id}.json
                             #   Pass 2 — warp all patients into shared global grid,
                             #            accumulate density volume
                             #   Visualizations: common basis / average liver / density slices
    vascular_distance.py     # Distance-to-vasculature electron cloud
                             #   Distances computed in native patient space, warped to atlas,
                             #   sampled on atlas consensus liver (≥50% density threshold)

  Segmentation/
    run_totalsegmentator.py  # Reads reviewed_all.csv, extracts one CT at a time from
                             #   Totalsegmentator_dataset_v201.zip, runs TotalSegmentator
                             #   (3 subtasks: ROI subset, liver_segments, liver_vessels),
                             #   writes segmentations to Data/segmentations.zip, deletes CT

  Validation/
    dataset_loader.py        # Reads reviewed CSVs, returns filtered patient ID lists
    validate_dataset.py      # Scans segmentations.zip for missing/empty/broken files
    visualize_registration.py # Before/after 3D mesh viewer for pairwise registration

  Data/                      # ← rename from Data(without CT)/ before running
    Dataset_Reviewed/
      reviewed_all.csv       # All 303 reviewed patients
      reviewed_male.csv      # 179 male patients
      reviewed_female.csv    # 122 female patients
      liver_flagged.csv      # Patients flagged for QC issues
    Totalsegmentator_dataset_v201.zip   # Raw CT source (not in GitHub — download separately)
    segmentations.zip                   # Generated by run_totalsegmentator.py (not in GitHub)

  outputs/
    reg_cache/
      rigid_{atlas_id}_{patient_id}.npz  # Cached per-patient rigid alignment
      extents_{atlas_id}.json            # Global bounding box + voxel counts + direction
                                         #   matrices for all patients — computed in Pass 1
    atlas_male/              # Male atlas outputs
      atlas_liver_density.nii.gz         # Average liver density volume
      01_common_basis.html               # Stage 1: all liver clouds in common space
      02_average_liver.html              # Stage 2: nested probability shells
      03_density_slices.html             # Stage 3: axial slice browser
      vdc_combined.html                  # Vascular distance cloud (combined)
      vdc_all_modes.html                 # Portal / hepatic / combined side by side
      vdc_slices.html                    # Distance axial slice browser
      vdc_histogram.html                 # Distance distribution sanity check
    atlas_female/            # Same structure as atlas_male

  run_pipeline_test.py       # End-to-end pipeline test on 10 male + 10 female patients
                             #   Checks segmentations.zip, runs TotalSegmentator if needed,
                             #   builds both atlases + distance clouds, saves all HTML outputs
                             #   Set LOAD_EXISTING = True to reload without recomputing
```

## Run Order

I created a quick full pipeline wrapper that does a small test set just for "quick" illustration's sake, it should still take some time to do the segmentations its about 3-5 mins per, so probably close to 2 hours or so for 20 people 

Also wanted to just say as well, to change the name of the data folder to be just Data/ I have just a secondary folder that's called that just so that I didn't accidentally upload CT scans to GitHub lol





## Key Design Decisions

**Rigid-only for the atlas** — distances are computed in each patient's native
space then rigidly warped. This avoids introducing TPS registration error into
the distance values. TPS is kept in Registration/stages/tps.py for evaluation
and comparison but is not called by the atlas pipeline.

**Shared cache** — `outputs/reg_cache/rigid_0004_{id}.npz` stores the Procrustes
alignment for each patient. Both `liver_atlas.py` and `vascular_distance.py` read
from the same cache so registration runs only once per patient total.

**LOAD_EXISTING flag** — set to True in each Atlas script after the first run
to reload saved outputs without re-running anything. I need to just add a check for this rather than req the operator to change it 

## Docker Compose

The repository root now includes a standalone Docker Compose setup that mounts
this Project directory into the container and maps `Data(without CT)` to the
`Data/` path the code expects.

Build the image:

```bash
docker compose build
```

Build the image and use the GPU override:

```bash
docker compose -f docker-compose.yml -f docker-compose.gpu.yml build
```

Run the atlas workflow in-container (equivalent to running
`python -m Atlas.liver_atlas` then `python -m Atlas.vascular_distance`
from `Project/`):

```bash
docker compose up atlas
```

Run the same atlas workflow with GPU access enabled:

```bash
docker compose -f docker-compose.yml -f docker-compose.gpu.yml up atlas
```

Compatibility alias (runs the same atlas workflow):

```bash
docker compose up pipeline
```

Run the pairwise registration CLI:

```bash
docker compose run --rm -e REF_ID=0004 -e SRC_ID=0010 registration
```

Open an interactive shell inside the container:

```bash
docker compose run --rm shell
```

Open an interactive shell with GPU access enabled:

```bash
docker compose -f docker-compose.yml -f docker-compose.gpu.yml run --rm shell
```

Notes:

- This setup is CPU-first. TotalSegmentator will run, but the full pipeline can
  still take a long time without GPU acceleration.
- The GPU override expects Docker Desktop to have GPU support available and the
  NVIDIA container runtime/toolkit installed on the host.
- Model downloads are cached in the named volume `totalsegmentator-cache`.
- Outputs written under `Project/outputs/` stay on the host because the entire
  `Project/` folder is bind-mounted into the container.
